EXPO_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
EXPO_PUBLIC_SITE_URL=https://your-app-domain.example
SITE_URL=https://your-app-domain.example

# Google sign-in is configured in Convex env, not as public Expo values:
# npx convex env set AUTH_GOOGLE_ID your_google_oauth_client_id
# npx convex env set AUTH_GOOGLE_SECRET your_google_oauth_client_secret
#
# Google OAuth redirect URI:
# https://your-project.convex.site/api/auth/callback/google

# Set this in Convex env, not in a public Expo variable:
# npx convex env set OPENAI_API_KEY your_api_key
# Optional:
# npx convex env set OPENAI_TUTOR_MODEL gpt-5.4-mini
